/* import { http } from './config.js'

export default class QuestionarioService{
    questionAll(){
        return http.get(`questionarios/`).then(result => result.data);
    }
    get(questionario) {
        return http.get(`questionarios/`, {params: {id: questionario.id}}).then(result => result.data);
      }
   
} */